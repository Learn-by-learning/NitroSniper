/// Nitro Sniper Bot ///
/* CODE: */

const nitro = require('discord.js-nitro');
const boost = true;   
message.channel.send(nitro(boost));
  
// Ejemplos:

// Usando IF:

if(command === "nitro"){
const nitro = require('discord.js-nitro');
const boost = true;   
message.channel.send(nitro(boost));
}

// Usando Client en Index.js:


const nitro = require('discord.js-nitro');
const boost = true;   
message.channel.send(nitro(boost));
